/**
 * Layout constants — mobile-first spacing for tab bar and safe area.
 * Ensures content is never cut off by bottom nav or floating elements.
 */
/** Bottom padding for scrollable content (tab bar ~56px + safe area + breathing room) */
export const BOTTOM_CONTENT_PADDING = 100;

/** FAB offset from bottom so it clears tab bar and safe area */
export const FAB_BOTTOM_OFFSET = 88;
